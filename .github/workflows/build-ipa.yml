name: Build IPA

on:
  workflow_dispatch:   # GitHub Actions画面から手動実行

jobs:
  build-ipa:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Cache Swift Package Manager
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-spm-${{ hashFiles('project.yml') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Create Recipe Database
        run: python3 scripts/create_db.py

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Resolve Swift Packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project HealthDiary.xcodeproj \
            -scheme HealthDiary

      - name: Build for iOS Device (unsigned)
        run: |
          BUILD_DIR="$(pwd)/build-device"
          xcodebuild build \
            -project HealthDiary.xcodeproj \
            -scheme HealthDiary \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            PROVISIONING_PROFILE_SPECIFIER="" \
            SKIP_INSTALL=NO \
            BUILD_DIR="$BUILD_DIR" \
            | xcpretty || xcodebuild build \
              -project HealthDiary.xcodeproj \
              -scheme HealthDiary \
              -sdk iphoneos \
              -destination 'generic/platform=iOS' \
              CODE_SIGNING_ALLOWED=NO \
              CODE_SIGN_IDENTITY="" \
              CODE_SIGNING_REQUIRED=NO \
              PROVISIONING_PROFILE_SPECIFIER="" \
              SKIP_INSTALL=NO \
              BUILD_DIR="$BUILD_DIR"

      - name: Package IPA
        run: |
          BUILD_DIR="$(pwd)/build-device"
          APP=$(find "$BUILD_DIR" -name "HealthDiary.app" -path "*/iphoneos*" | head -1)
          if [ -z "$APP" ]; then
            echo "ERROR: HealthDiary.app not found"
            find "$BUILD_DIR" -name "*.app" 2>/dev/null
            exit 1
          fi
          echo "Found: $APP"
          mkdir -p Payload
          cp -r "$APP" Payload/HealthDiary.app
          zip -qr HealthDiary.ipa Payload/
          rm -rf Payload
          echo "IPA size: $(du -sh HealthDiary.ipa | cut -f1)"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: HealthDiary-unsigned
          path: HealthDiary.ipa
          retention-days: 30
